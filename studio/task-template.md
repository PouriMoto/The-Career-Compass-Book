# قرارداد یک task برای AI

هدف:

ورودی: فقط فایل‌های مرتبط، نسخه Studio، schema و محدودیت‌های محصول.

رفتار مورد انتظار و یک مثال:

موتور/کامپوننت موجود برای reuse:

معیار پذیرش:

آزمون خطا، بازیابی داده، RTL و keyboard متناسب با تغییر:

خروجی: کد، شواهد اجرای تست، محدودیت‌ها و به‌روزرسانی registry.

پایان فاز: فایل‌های تغییرکرده، تصمیم پایدار، موارد باز و اولین کار بعدی را در یک خلاصه کوتاه ثبت کن. کاتالوگ بزرگ را کامل وارد context نکن؛ ابتدا با id یا use case جست‌وجو کن.
